import styled from 'styled-components';

const SMainLogin = styled.main`
  margin-top: 5rem;
  text-align: center;

  p {
    color: gray;
    font-size: 13px;
    margin-top: 1rem;
  }
`;

export default SMainLogin;
